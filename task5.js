/* ৫. জাভাস্ক্রিপ্ট এ array এর map, forEach, filter, find কোনটা দিয়ে কি হয়। সেটার একটা সামারি লিখে ফেলো।  */

/* 
map(): is used for run a loop throgh an array and apply some calculation on every elements of that array.
filter(): is used for run a loop through an array and it will filter and give the elements of matching elements with the given function condition.
find(): it will find only the element matching with given condition. and only give the first element only. will not go for next matching elements in the array which is done by filter();
*/